services:
  - type: web
    name: pulse-review-python-code-intelligence
    runtime: docker
    plan: free
    healthCheckPath: /health
    envVars:
      - key: DATABASE_PATH
        value: /data/reviews.db